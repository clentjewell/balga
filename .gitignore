# Dependencies
node_modules/

# Build output
dist/
.astro/

# Cloudflare / Wrangler local state
.wrangler/
.dev.vars
wrangler.local.jsonc

# Env / secrets
.env
.env.*
!.env.example

# Playwright
test-results/
playwright-report/
blob-report/

# Large local working artifacts (site mirror + QA screenshots)
# These are generated locally during audit/QA and are intentionally not committed
# because they are large binary captures. See audit/ for the committed deliverables.
_audit/
qa/

# OS / editor
.DS_Store
Thumbs.db
*.log
